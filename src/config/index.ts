import appConfig from "./app.config";
import redisConfig from "./redis.config";
import databaseConfig from "./database.config";
import hisConfig from "./his.config";
import tokenConfig from "./token.config";

export const Config = [
  appConfig,
  redisConfig,
  databaseConfig,
  hisConfig,
  tokenConfig,
];
